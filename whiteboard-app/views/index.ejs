<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="/javascripts/index.js"></script>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to my simple whiteboard. I used Node.js as the server, Express as the web framework and HTML5 Canvas for the whiteboard. My name is also <%= name %>.</p>
    <div id="toolbar">
    </div>
    <canvas id="canvas" width="500" height="500"></canvas>

    <output style="border: 2px red solid; width: 100px; height: 100px; display: block;"></output>
    <div>
      <label for="message">Messages:</label>
      <input id="message" type="text"/>
      <button id="button">Send</button>
    </div>
    <script>
      

      // socket.on('draw', function(data) {
      //   console.log("data")
      // });

      // $('canvas').submit(function(){
      // socket.emit('chat message', $('#message').val());
      // $('#message').val('');
      // return false;
      // });
      // socket.on('chat message', function(message){
      //   $('#messages').append($('<li>').text(message));
      // });
    </script>
    <script src="https://cdn.socket.io/socket.io-1.3.5.js"></script>
  </body>   
</html>
